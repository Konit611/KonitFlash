import Foundation

struct StudyResult {
    let totalCards: Int
    let correctCount: Int
    let againCount: Int
    let hardCount: Int
    let goodCount: Int
    let easyCount: Int
    let elapsedSeconds: Double
}
